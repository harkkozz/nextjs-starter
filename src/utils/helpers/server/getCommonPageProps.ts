import type { GetServerSidePropsContext } from 'next';

export const getCommonPageProps = async (context: GetServerSidePropsContext): Promise<void> => {
  // return {
  //   // return common props from context
  // };
};

export default getCommonPageProps;
